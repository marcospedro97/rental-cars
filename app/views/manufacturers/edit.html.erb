<section class="section">
  <div class="container">
    <div class="columns is-centered">
      <div class="column is-4">
        <h5 class="title is-5">Atualizar fabricante</h1>
          <% if @manufacturer.errors.any? %>
            <p>Você deve corrigir os seguintes erros:</p>
            <ul>
            <% @manufacturer.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
            </ul>
          <% end %>
        <%= form_with model: @manufacturer, local: true do |f| %>
          <div class="field">
            <div class="control">
              <%= f.label :name, 'Nome' %><br />
              <%= f.text_field :name, class:"input is-primary" %><br />
            </div>
          </div>
            <%= f.submit 'Enviar', class:"button is-info is-medium" %>
          </p>
        <% end %>
      </div>
    </div>
  </div>
</section>
